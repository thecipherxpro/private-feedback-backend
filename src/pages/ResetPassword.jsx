import React,{useState}from'react';import{Link,useSearchParams}from'react-router-dom';import{base44}from'@/api/base44Client';import{Button}from'@/components/ui/button';import{Input}from'@/components/ui/input';import{Label}from'@/components/ui/label';import AuthLayout from'@/components/AuthLayout';export default function ResetPassword(){const[params]=useSearchParams();const token=params.get('token');const[p1,setP1]=useState('');const[p2,setP2]=useState('');const[error,setError]=useState('');async function submit(e){e.preventDefault();if(p1!==p2){setError('Passwords do not match');return}try{await base44.auth.resetPassword({resetToken:token,newPassword:p1});window.location.href='/login'}catch(err){setError(err.message||'Failed to reset password')}}if(!token)return <AuthLayout title="Invalid reset link" footer={<Link to="/forgot-password" className="underline">Request a new link</Link>}/>;return <AuthLayout title="New password">{error&&<p className="mb-4 text-sm text-destructive">{error}</p>}<form onSubmit={submit} className="space-y-4"><div className="space-y-2"><Label>New password</Label><Input type="password" value={p1} onChange={e=>setP1(e.target.value)} className="h-12"/></div><div className="space-y-2"><Label>Confirm password</Label><Input type="password" value={p2} onChange={e=>setP2(e.target.value)} className="h-12"/></div><Button className="h-12 w-full">Reset password</Button></form></AuthLayout>}