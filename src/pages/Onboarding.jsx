import React,{useEffect,useState}from'react';import{useNavigate}from'react-router-dom';import{Button}from'@/components/ui/button';import{Input}from'@/components/ui/input';import{Checkbox}from'@/components/ui/checkbox';import{Card,CardContent}from'@/components/ui/card';import{Label}from'@/components/ui/label';import{ShieldCheck,Loader2}from'lucide-react';import{api}from'@/lib/backend';export default function Onboarding(){const n=useNavigate();const saved=JSON.parse(sessionStorage.getItem('pf_onboarding')||'{}');const[nickname,setNickname]=useState(saved.nickname||'');const[phone,setPhone]=useState(saved.phone||'');const[consent,setConsent]=useState(saved.consent!==false);const[sent,setSent]=useState(false);const[code,setCode]=useState('');const[busy,setBusy]=useState(false);const[error,setError]=useState('');useEffect(()=>{api('account').then(r=>{if(r.profile?.phone_verified)n('/',{replace:true})}).catch(()=>{})},[]);async function request(){setBusy(true);setError('');try{await api('request_phone_otp',{phone});setSent(true)}catch(e){setError(e.message)}finally{setBusy(false)}}async function verify(){setBusy(true);setError('');try{await api('verify_phone_otp',{code,nickname,sms_consent:consent});sessionStorage.removeItem('pf_onboarding');n('/',{replace:true})}catch(e){setError(e.message)}finally{setBusy(false)}}return <div className="min-h-screen bg-[#f6f7f9] p-4"><div className="mx-auto flex min-h-[90vh] max-w-md items-center"><Card className="w-full shadow-none"><CardContent className="p-6"><ShieldCheck/><h1 className="mt-5 text-2xl font-semibold">Verify your mobile</h1><p className="mt-1 text-sm text-muted-foreground">We’ll text a 6-digit code.</p>{error&&<p className="mt-4 rounded-lg bg-destructive/10 p-3 text-sm text-destructive">{error}</p>}{!sent?<div className="mt-6 space-y-4"><div className="space-y-2"><Label>Nickname</Label><Input value={nickname} onChange={e=>setNickname(e.target.value)} className="h-11"/></div><div className="space-y-2"><Label>Canadian mobile number</Label><Input value={phone} onChange={e=>setPhone(e.target.value)} className="h-11"/></div><label className="flex items-start gap-3 rounded-xl border p-3"><Checkbox checked={consent} onCheckedChange={v=>setConsent(!!v)}/><span className="text-xs text-muted-foreground">I agree to receive SMS notifications.</span></label><Button className="h-11 w-full" disabled={busy||!nickname||!phone} onClick={request}>{busy?<Loader2 className="animate-spin"/>:'Send verification code'}</Button></div>:<div className="mt-6 space-y-4"><Label>6-digit code</Label><Input value={code} onChange={e=>setCode(e.target.value.replace(/\D/g,'').slice(0,6))} className="h-12 text-center text-xl tracking-[.35em]"/><Button className="h-11 w-full" disabled={busy||code.length!==6} onClick={verify}>{busy?<Loader2 className="animate-spin"/>:'Verify & continue'}</Button></div>}</CardContent></Card></div></div>}